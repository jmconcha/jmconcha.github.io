document.addEventListener('DOMContentLoaded', ()=>{const links=document.querySelectorAll('header a');const btn=document.querySelector('.header-btn');const header=document.querySelector('header');function toggleHeader(){header.classList.toggle('header-active');}for (let link of links){link.addEventListener('click', ()=>{for (let link of links){link.className='';}link.className='active';if (header.className.endsWith('header-active')){toggleHeader()}});};btn.addEventListener('click', ()=>{toggleHeader();});});